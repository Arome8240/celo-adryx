DATABASE_URL="postgresql://user:password@host:5432/dbname"

JWT_ACCESS_SECRET=""
JWT_REFRESH_SECRET=""

CORS_ORIGINS="http://localhost:3000"

# SIWE — must match what the frontend sends as the `domain`/`uri` in the message.
SIWE_DOMAIN="localhost:3000"
SIWE_URI="http://localhost:3000"

AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
# "test" or "production" — always explicit, never left to default.
AMADEUS_ENV=test

# Added in later phases (payments) — not needed for Phase 0/1/2/3:
# CELO_RPC_URL=https://forno.celo.org
# CHAIN_ID=42220
# ESCROW_CONTRACT_ADDRESS=
# CUSD_TOKEN_ADDRESS=0x765DE816845861e75A25fCA122bb6898B8B1282a
# OPERATOR_PRIVATE_KEY=
# TREASURY_ADDRESS=
